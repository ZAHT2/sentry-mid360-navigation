# MID360 宿主机建图流程

这份文档是唯一的日常建图入口。建图运行在宿主机 `/home/zaht/sentry_nav_ws`，不再把 Docker 作为正式流程；Docker 只保留为历史备份。

核心链路：

```text
MID360 -> livox_ros_driver2 -> Point-LIO -> sensor_scan_generation -> slam_toolbox -> RViz
```

固定实车 TF：

```text
base_footprint -> front_mid360:
  x=-0.001, y=+0.17185, z=0.200
  roll=0.7854, pitch=0, yaw=0

base_footprint -> gimbal_yaw:
  identity

gimbal_yaw -> gimbal_yaw_fake:
  identity
```

## 0. 关键字说明

```text
source
  加载 ROS2 和当前工作空间环境。没有 source 时，ros2 找不到本地包。

FASTDDS_BUILTIN_TRANSPORTS=UDPv4
RMW_FASTRTPS_USE_SHM=0
  禁用 FastDDS 共享内存，避开 fastrtps_port 残留导致的半启动。

mid360_mapping.launch.py
  宿主机建图入口。它发布实车静态 TF，并启动开源建图后端。

use_composition:=False
  让关键节点独立运行，方便看日志、调参和排错。

MAP_ID
  地图编号。MAP_ID=14 会保存 real_map14.yaml、real_map14.pgm、real_map14.png、real_scans14.pcd。

PCD
  3D 点云地图。Point-LIO 退出时写到 src/point_lio/PCD/scans.pcd。

PGM / PNG / YAML
  PGM 是 Nav2 使用的 2D 栅格图，PNG 是人看的预览图，YAML 记录地图分辨率、原点和 image 文件名。
```

## 1. 终端 1：清理并启动建图

启动前清理旧 ROS 进程和 DDS 残留：

```bash
cd /home/zaht/sentry_nav_ws

pkill -9 -f '[m]id360_mapping|[r]m_navigation_reality|[r]viz2|[c]hassis_bridge_node|[p]ointlio_mapping|[s]mall_gicp_relocalization_node|[l]ivox_ros_driver2_node|[s]tatic_transform_publisher|[l]oam_interface_node|[s]ensor_scan_generation_node|[s]ync_slam_toolbox_node|[p]ointcloud_to_laserscan_node|[t]errainAnalysis|[t]errainAnalysisExt|[m]ap_saver_server|[l]ifecycle_manager|[m]ap_server|[c]ontroller_server|[p]lanner_server|[b]t_navigator|[v]elocity_smoother|[b]ehavior_server|[s]moother_server|[w]aypoint_follower|[j]oy_node|[p]b_teleop_twist_joy_node|[c]omponent_container_isolated' || true

export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
source /opt/ros/humble/setup.bash
ros2 daemon stop || true
rm -f /dev/shm/fastrtps_* /dev/shm/sem.fastrtps* /dev/shm/fastdds* /dev/shm/sem.fastdds* 2>/dev/null || true
```

确认 MID360 网络。宿主机有线口使用 `enp3s0`，宿主机 IP 使用 `192.168.1.50/24`，当前雷达 IP 使用 `192.168.1.104`。

```bash
sudo ip link set enp3s0 up
sudo ip addr replace 192.168.1.50/24 dev enp3s0
ping -c 1 -W 1 192.168.1.104
```

启动宿主机建图：

```bash
cd /home/zaht/sentry_nav_ws

export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 launch sentry_bringup mid360_mapping.launch.py use_rviz:=True use_composition:=False
```

启动后静置 `3-5 秒`，等 IMU 和 Point-LIO 初始化完成，再开始慢速扫图。不要甩雷达，不要一启动就移动。

## 2. 终端 2：巡检

```bash
cd /home/zaht/sentry_nav_ws

export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 topic info /map
timeout 5 ros2 topic hz /livox/lidar
timeout 5 ros2 topic hz /cloud_registered
timeout 5 ros2 run tf2_ros tf2_echo odom gimbal_yaw_fake
```

正确输出：

```text
/map:
  Type: nav_msgs/msg/OccupancyGrid
  Publisher count: 1

/livox/lidar:
  average rate: 20 左右

/cloud_registered:
  average rate: 20 左右

odom -> gimbal_yaw_fake:
  Translation 第三个数 z = 0
  Rotation RPY 里 roll = 0，pitch = 0
  yaw 可以小幅变化
```

启动初期可能先出现：

```text
Waiting for transform odom -> gimbal_yaw_fake: Invalid frame ID "odom"
```

如果后面开始连续输出 `Translation` 和 `Rotation`，这是正常等待，不算错误。只有几秒内一直没有 transform，才说明 Point-LIO 或 TF 主链没有接上。

额外确认独立进程：

```bash
ps -eo pid,cmd | grep -E 'sensor_scan_generation_node|component_container_isolated|pointlio_mapping|sync_slam_toolbox_node|rviz2' | grep -v grep
```

正确状态：

```text
能看到 sensor_scan_generation_node。
不应依赖 component_container_isolated。
```

## 3. 扫图动作

```text
慢走或慢推。
通道、拐角、障碍物边缘多扫一遍。
尽量回到起点附近形成回环。
不要长时间只扫一面完整墙，特征少时容易拉丝。
发现点云明显拉丝、地图整体倾斜或 TF 抖动，立刻停止并重建。
```

## 4. 保存地图

建图完成后，在终端 2 保存 2D 地图。下面示例保存成 `map14`，下一次只改 `MAP_ID`。

```bash
cd /home/zaht/sentry_nav_ws

MAP_ID=15
mkdir -p /home/zaht/sentry_nav_ws/real_maps

source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 run nav2_map_server map_saver_cli -f /home/zaht/sentry_nav_ws/real_maps/real_map${MAP_ID} --ros-args -p save_map_timeout:=10.0 -p free_thresh_default:=0.25 -p occupied_thresh_default:=0.65
```

保存 2D 地图成功后，回到终端 1 按 `Ctrl-C` 停止建图。Point-LIO 会在退出时保存 3D 点云到：

```text
/home/zaht/sentry_nav_ws/src/point_lio/PCD/scans.pcd
```

复制 3D 点云并生成 PNG：

```bash
cd /home/zaht/sentry_nav_ws

MAP_ID=15

cp /home/zaht/sentry_nav_ws/src/point_lio/PCD/scans.pcd /home/zaht/sentry_nav_ws/real_maps/real_scans${MAP_ID}.pcd

sed -i "s#^image: .*#image: real_map${MAP_ID}.pgm#" /home/zaht/sentry_nav_ws/real_maps/real_map${MAP_ID}.yaml

python3 - <<PY
from pathlib import Path
from PIL import Image
map_id = "${MAP_ID}"
src = Path(f"/home/zaht/sentry_nav_ws/real_maps/real_map{map_id}.pgm")
dst = Path(f"/home/zaht/sentry_nav_ws/real_maps/real_map{map_id}.png")
Image.open(src).save(dst)
print(dst)
PY
```

检查结果：

```bash
MAP_ID=15

ls -lh /home/zaht/sentry_nav_ws/real_maps/real_map${MAP_ID}.yaml \
       /home/zaht/sentry_nav_ws/real_maps/real_map${MAP_ID}.pgm \
       /home/zaht/sentry_nav_ws/real_maps/real_map${MAP_ID}.png \
       /home/zaht/sentry_nav_ws/real_maps/real_scans${MAP_ID}.pcd
```

## 5. 常见问题

### RViz 空白

处理：

```text
先确认 tf2_echo odom gimbal_yaw_fake 是否能输出。
再确认 sensor_scan_generation_node 是否存在。
如果 TF 断开，执行第 1 节清理后重启。
```

### `/livox/lidar` 无数据

处理：

```text
检查雷达供电、网线、主机 IP、雷达 IP。
如果 enp3s0 显示 NO-CARRIER 或 carrier 为 0，说明物理链路没起来。
不要先怀疑 SLAM，先修网络链路。
```

检查：

```bash
ip -brief addr show enp3s0
cat /sys/class/net/enp3s0/carrier
ping -c 1 -W 1 192.168.1.104
```

### `/cloud_registered` 无数据

处理：

```text
Point-LIO 未正常输出，先看终端 1 的 Point-LIO 日志。
如果 /livox/lidar 和 /livox/imu 都正常，再检查 Point-LIO 是否完成 IMU 初始化。
```

### 保存成 `real_map.yaml`

原因：

```text
MAP_ID 没设置，或设置在另一个终端。
```

处理：

```text
保存前必须在同一个终端执行 MAP_ID=数字。
已经误保存时，补救后要同步修 YAML 的 image 字段。
```

### PCD 没更新

原因：

```text
保存 2D 地图后没有 Ctrl-C 停止建图，Point-LIO 还没把 scans.pcd 写出。
```

处理：

```text
先保存 2D。
再 Ctrl-C 停建图。
最后复制 src/point_lio/PCD/scans.pcd。
```

### 点云倾斜或地图被掀起来

检查：

```bash
timeout 5 ros2 run tf2_ros tf2_echo odom gimbal_yaw_fake
```

正确：

```text
Translation z = 0
Rotation roll = 0
Rotation pitch = 0
```

如果不满足，不能继续扫图，必须清理重启并确认 `sensor_scan_generation.cpp` 的 2D 展平补丁存在。
