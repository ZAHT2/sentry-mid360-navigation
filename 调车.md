# MID360 哨兵底盘调车记录

本文记录本工作区实车导航调试期间遇到的问题、原因、处理方法、修改过的参数，以及后续测试顺序。

> 当前参数以 `src/pb2025_nav_bringup/config/reality/nav2_params.yaml` 的磁盘内容为准。历史测试值只用于对比，不代表最终安全配置。

## 一、系统链路

```text
Livox MID360
  -> /livox/lidar
  -> Point-LIO
  -> /cloud_registered
  -> loam_interface
  -> /registered_scan
  -> terrain_analysis
  -> /terrain_map
  -> IntensityVoxelLayer
  -> local/global costmap
  -> ThetaStar 全局规划
  -> OmniPidPursuitController
  -> velocity_smoother
  -> chassis_bridge_node
  -> /dev/ttyACM0
```

Nav2 使用的 `/odometry` 由 Point-LIO 激光惯性里程计生成，不是底盘轮式里程计。底盘另行发布 `/chassis/odom_raw`，但当前没有融合进 Nav2 `/odometry`。

## 二、标准启动方式

```bash
cd /home/rciazzx/zaht/sentry-mid360-navigation

export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 launch sentry_bringup map11_navigation.launch.py \
  use_rviz:=True \
  use_composition:=False \
  map:=$PWD/maps/zd.yaml \
  prior_pcd_file:=$PWD/maps/scans.pcd \
  serial_port:=/dev/ttyACM0
```

启动后至少等待 20-30 秒，再发送导航目标。

## 三、已出现的问题及原因

### 1. 底盘不动、没有里程计

现象：

```text
/chassis/odom_raw 无数据
Failed to make progress
```

原因：曾使用不存在的 `/dev/ttyABoard`。

正确设备：

```text
/dev/ttyACM0
115200 baud
```

正常情况下 `/chassis/odom_raw` 约为 50 Hz。

### 2. 雷达启动初期提示无法分配索引

出现过：

```text
Storage point data failed
Can not get index
found lidar not defined in the user-defined config
```

配置中已经包含雷达 IP `192.168.1.104`，主机接收 IP 为 `192.168.1.50`。如果随后成功进入 Normal 模式、启用 IMU，且 `/livox/lidar` 保持约 20 Hz，通常是设备索引注册前的瞬时日志。

### 3. 使用错误的先验点云导致 GICP 崩溃

错误参数：

```text
prior_pcd_file:=maps/zd.pcd
```

`maps/zd.pcd` 不存在，曾导致：

```text
small_gicp_relocalization exit code -11
```

正确文件：

```text
maps/scans.pcd
```

### 4. Nav2 节点 inactive/unconfigured

出现过：

```text
controller_server: inactive [2]
planner_server: inactive [2]
bt_navigator: unconfigured [1]
```

原因包括：

- lifecycle manager 延迟 15 秒启动；
- 用户过早按 Ctrl+C；
- DDS 服务响应超时；
- 某个节点 configure 卡住。

检查：

```bash
ros2 lifecycle get /map_server
ros2 lifecycle get /controller_server
ros2 lifecycle get /planner_server
ros2 lifecycle get /bt_navigator
```

必要时只执行一次 startup：

```bash
ros2 service call /lifecycle_manager_navigation/manage_nodes \
  nav2_msgs/srv/ManageLifecycleNodes "{command: 0}"
```

不要连续执行 reset 和 startup。曾发生 reset 延迟执行，在车辆开始导航后把整套 Nav2 反激活，随后重复 configure 又报：

```text
parameter 'intensity_voxel_layer.z_voxels' has already been declared
```

### 5. 路点发送后没有路径

典型日志：

```text
Either of the start or goal pose are an obstacle!
```

可能原因：

- GICP 定位错位，机器人起点落入静态障碍；
- 路点放在障碍或膨胀区域；
- 第一个路点离机器人过近；
- footprint 或 inflation radius 过大，窄区域没有可行空间。

曾有路点距离机器人约 0.24 m，小于车体碰撞范围，因此不适合作为首个路点。

### 6. GICP 持续拒绝更新

反复出现：

```text
Keeping previous map->odom transform after rejected GICP update
reason=error_too_high
```

误差曾在 20-50，严重时超过 200，并出现约 0.4 m 的 translation jump。直接影响：

- 白色实时点云与地图错位；
- 起点被判为障碍；
- Nav2 错误判断已到达；
- 路径规划和实时避障不稳定。

应先校准 GICP `init_pose` 或确认 RViz `2D Pose Estimate` 确实更新了 `map -> odom`，不能靠缩小碰撞体积掩盖定位错误。

### 7. 车辆走约 4 秒后停车

一次案例中动作状态为：

```text
status: 4
```

`4` 表示 SUCCEEDED，不是崩溃。Nav2 根据错误或过近的定位位姿认为所有路点已经到达，因此主动输出零速度。

另一次停车由 lifecycle reset/startup 竞态造成，不应混为控制器避障问题。

### 8. Failed to make progress

当前进度检查参数：

```yaml
required_movement_radius: 0.5
movement_time_allowance: 10.0
```

含义是在 10 秒内至少移动 0.5 m，否则控制器报告无进展。常见原因是障碍挡路、定位不动、底盘未执行速度或路径不可跟踪。

### 9. RViz GLSL 报错

```text
active samplers with a different type refer to the same texture image unit
```

通常只是 OpenGL 地图纹理显示问题，不是 Nav2 规划失败原因。

## 四、近距离障碍识别调试

### 1. 0.5 m 内障碍识别不到

直接原因是 Point-LIO 原配置：

```yaml
blind: 0.5
```

它会过滤 0.5 m 内的点。当前已改为：

```yaml
blind: 0.20
```

建议依次在 0.2、0.3、0.4、0.5 m 放置障碍，确认 `/cloud_registered` 可见。

### 2. 点云链路检查顺序

```text
/cloud_registered
  -> /terrain_map
  -> /local_costmap/costmap
  -> /cmd_vel_controller
```

正常频率实测：

```text
/cloud_registered 约 20 Hz
/terrain_map       约 20 Hz
/chassis/odom_raw  约 50 Hz
```

频率正常只代表链路活着，不代表障碍点一定通过了过滤。

### 3. `/cloud_registered` 有障碍，但 `/terrain_map` 没有

关联参数：

```yaml
terrain_analysis:
  scanVoxelSize: 0.05
  decayTime: 0.2
  clearDyObs: True
  minBlockPointNum: 10
  vehicleHeight: 0.5
  minRelZ: -1.5
  maxRelZ: 0.5
  disRatioZ: 0.2
```

历史建议测试值：

```yaml
clearDyObs: False
decayTime: 0.5       # 或 0.8，增加障碍保留时间
minBlockPointNum: 3  # 近距离点数少时降低门槛
```

`clearDyObs: True` 会清除被算法判为动态物体的点。车辆运动时，静态物体也可能因点云畸变、TF或里程计误差被误判为动态物体。

### 4. `/terrain_map` 有障碍，但局部代价地图没有

当前局部参数：

```yaml
min_obstacle_intensity: 0.1
max_obstacle_intensity: 2.0
obstacle_min_range: 0.05
obstacle_max_range: 5.0
min_obstacle_height: 0.0
max_obstacle_height: 2.0
mark_threshold: 0
```

这里的 intensity 是障碍点相对地面的高度差，不是 Livox 原始反射强度。

历史测试建议：

```yaml
min_obstacle_intensity: 0.05
obstacle_min_range: 0.05
```

每次只修改一项，避免无法判断是哪一个参数生效。

## 五、车体和代价地图参数

底盘实际尺寸确认是 60 cm × 60 cm。当前局部和全局代价地图均使用正方形 footprint：

```yaml
footprint: "[[0.30, 0.30], [0.30, -0.30], [-0.30, -0.30], [-0.30, 0.30]]"
```

此前使用过圆形近似：

```yaml
robot_radius: 0.32
```

60 cm 正方形的外接圆半径约为 0.424 m，但精确 footprint 比圆形近似更适合窄道。

当前局部和全局膨胀参数：

```yaml
cost_scaling_factor: 3.0
inflation_radius: 0.55
```

参数作用：

- `footprint`：真实碰撞边界，不应小于真实车身；
- `inflation_radius`：障碍外围代价影响距离；
- `cost_scaling_factor`：数值越大，代价随距离下降越快，路径越贴近障碍。

曾测试/讨论过的 inflation radius：1.0、0.8、0.7、0.65、0.55、0.5 m。不要通过缩小 footprint 来解决路径离障碍太远，应调 inflation layer。

## 六、当前速度参数

当前为激进档：

```yaml
FollowPath:
  min_approach_linear_velocity: 0.8
  approach_velocity_scaling_dist: 0.5
  v_linear_max: 3.5
  v_linear_min: -2.5
  v_angular_max: 3.5
  v_angular_min: -3.5
  reduction_ratio_at_high_curvature: 0.7

velocity_smoother:
  max_velocity: [3.5, 3.5, 3.5]
  min_velocity: [-2.5, -2.5, -3.5]
  max_accel: [5.0, 5.0, 6.0]
  max_decel: [-5.0, -5.0, -6.0]
```

历史上测试或讨论过：

- 线速度上限：0.8、2.0、2.5、3.0、3.5、5.0 m/s；
- 反向速度下限：-0.8、-1.2、-2.0、-2.5 m/s；
- 角速度：±1.2、±2.0、±3.0、±3.5 rad/s；
- 线加减速度：0.8、1.5、2.0、4.5、5.0 m/s²；
- 角加减速度：1.5、3.0、5.0、6.0 rad/s²。

控制器和 velocity smoother 的速度限制必须同步，否则会被较小的一层限速。`v_linear_min` 是反向速度下限，必须写负数。

## 七、靠近终点和绕障速度

接近终点速度由以下参数控制：

```yaml
min_approach_linear_velocity: 0.8
approach_velocity_scaling_dist: 0.5
```

- `approach_velocity_scaling_dist`：距离终点多远开始降速；
- `min_approach_linear_velocity`：接近终点时的最低速度。

不要为了表达“最低正常速度”把该值设成 1.2 或 2.0，否则可能高速冲过终点。

绕障弯道速度由以下参数控制：

```yaml
curvature_min: 0.4
curvature_max: 0.7
reduction_ratio_at_high_curvature: 0.7
curvature_forward_dist: 0.7
curvature_backward_dist: 0.3
max_velocity_scaling_factor_rate: 0.9
```

`reduction_ratio_at_high_curvature: 0.7` 表示高曲率时保留约 70% 线速度。

## 八、预测和碰撞检测

当前预测/前视参数：

```yaml
use_velocity_scaled_lookahead_dist: true
lookahead_time: 1.0
min_lookahead_dist: 0.5
max_lookahead_dist: 1.0
```

历史建议测试值：

```yaml
lookahead_time: 1.2
max_lookahead_dist: 1.5
```

注意：当前 OmniPidPursuitController 没有按障碍距离连续降速的功能。它会沿局部路径固定抽样 10 个点：

```text
路径未碰撞 -> 正常跟踪
预测路径碰撞 -> 抛出 Collision detected，停车并重新规划
```

真正绕行依赖行为树以 3 Hz 重新执行全局规划。`behavior_server.simulate_ahead_time` 主要用于恢复行为，不是正常 FollowPath 的障碍预测参数。

如果需要“距障碍 1.5 m 开始平滑减速、0.5 m 停车”，现有控制器参数不能完整实现，需要扩展控制器的障碍距离到速度映射，或替换为支持碰撞时间调速的控制器。

## 九、推荐调试流程

1. 固定低速 0.5-1.0 m/s，先验证传感链路。
2. 检查 `/cloud_registered` 是否能看到近距离障碍。
3. 检查 `/terrain_map` 是否保留静态和动态障碍。
4. 检查 `/local_costmap/costmap` 是否及时生成代价区域。
5. 检查控制器是否停车、BT是否重新规划。
6. 移走障碍，测量清除时间并调 `decayTime`。
7. 用相同路线和障碍位置逐级提速：1.0、1.5、2.0、2.5、3.0 m/s。
8. 记录发现距离、停车距离、最小间隙、重规划耗时和误报次数。
9. 定位和避障稳定后才测试 3.5 m/s 激进档。

每次只改一个参数。参数优先级：

```text
blind
-> clearDyObs / decayTime / minBlockPointNum
-> obstacle intensity/range
-> footprint / inflation
-> lookahead / curvature
-> speed / acceleration
```

## 十、禁止事项

- 不要使用不存在的 `maps/zd.pcd`。
- 不要使用 `/dev/ttyABoard`。
- 不要连续调用 lifecycle reset 和 startup。
- 不要在 Nav2 尚未 active 时发送路点。
- 不要只看话题频率判断障碍已经被识别。
- 不要缩小 footprint 到小于真实 60 cm 车身。
- 不要在 GICP 持续 `error_too_high` 时用缩小碰撞范围掩盖定位问题。
- 不要只修改控制器速度而不修改 velocity smoother。
- 不要同时修改多个避障参数。
- 不要直接以 3.5 m/s 开始未知障碍测试，必须准备急停并逐级提速。
