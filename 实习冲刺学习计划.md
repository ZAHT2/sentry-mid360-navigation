# MID360/Nav2 实习冲刺学习计划

这份计划用于把当前 `sentry_nav_ws` 项目打磨成实习敲门砖。目标不是重新学一套脱离项目的课程，而是围绕已经跑通的宿主机建图、低速导航、实时避障和底盘桥，把能力补成企业能认可的导航工程能力。

默认节奏：每周 `40 小时以上`。主攻岗位：

- ROS2 导航工程实习
- 机器人软件工程实习
- SLAM / 导航算法工程实习

## 0. 当前项目基线

当前已经具备的底层能力：

- 宿主机 MID360 建图流程稳定，入口见 `mid360_宿主机建图流程.md`。
- 宿主机低速导航流程稳定，入口见 `mid360_导航固定流程.md`。
- 固定云台 TF 已验证：`base_footprint -> front_mid360` 使用 `y=+0.17185`、`roll=0.7854`。
- 定位以 MID360 / Point-LIO / small_gicp 为主，底盘轮式里程计只发布 `/chassis/odom_raw` 做监控。
- 动态障碍不再污染重定位：定位点云使用 `/registered_scan`，避障使用 terrain costmap 点云。

后续学习全部基于这套真实项目展开。

## 1. 八周总目标

8 周结束时，你应该能做到：

- 现场启动建图、导航、实时避障，并解释每个终端命令为什么存在。
- 画出从 MID360 到 A 板的完整 ROS2 / TF / Nav2 / 串口链路。
- 解释 `topic`、`action`、`lifecycle`、`QoS`、`tf2` 在本项目中的具体作用。
- 讲清楚 Point-LIO、注册点云、GICP 重定位、costmap 实时避障分别解决什么问题。
- 用 4 到 6 条简历 bullet 描述项目成果，而不是只写“调通 Nav2”。

## 2. 每周计划

### 第 1 周：ROS2 通信与 TF 能力补齐

学习重点：

- `topic`、`service`、`action`、`parameter`、`lifecycle`、`QoS`、`launch`、`tf2`。
- 搞清楚为什么 `/cmd_vel` 是 topic，Nav2 Goal 是 action，Nav2 服务器有 lifecycle。

项目动作：

- 对照 `mid360_导航固定流程.md`，列出所有关键 topic、action、node、frame。
- 用 `ros2 topic info`、`ros2 action list -t`、`ros2 node info`、`tf2_echo` 验证链路。
- 画一张“雷达到电机”的数据流图。

产出：

- `ROS2链路说明.md`
- 一张从 `MID360 -> Point-LIO -> Nav2 -> chassis_bridge_node -> A板` 的链路图
- 一段 5 分钟讲解稿

验收标准：

- 能解释 `/livox/lidar`、`/cloud_registered`、`/map`、`/cmd_vel`、`/chassis/odom_raw` 各自是什么。
- 能解释 `map -> odom -> base_footprint -> front_mid360` 为什么不能乱。

### 第 2 周：Nav2 核心链路拆解

学习重点：

- `map_server`
- `planner_server`
- `controller_server`
- `bt_navigator`
- `local_costmap` / `global_costmap`
- `velocity_smoother`
- `waypoint_follower`

项目动作：

- 对照启动日志和 lifecycle 状态，整理每个 Nav2 节点的职责。
- 记录正常状态：`active [3]`、`/map Publisher count: 1`、`/cmd_vel Subscription count >= 1`。
- 复盘一次完整 goal：从 RViz 点击目标到 `/cmd_vel` 输出。

产出：

- `Nav2节点职责表.md`
- 一张 Nav2 内部链路图

验收标准：

- 能说明全局规划、局部控制、代价地图、行为树分别负责什么。
- 能解释为什么 lifecycle 处于 `inactive/unconfigured` 时不能点 goal。

### 第 3 周：实时避障专项

学习重点：

- `obstacle_layer`
- `inflation_layer`
- `robot_radius` / `footprint`
- `obstacle_range`
- `raytrace_range`
- local costmap 和 global costmap 的区别

项目动作：

- 复现实车动态障碍测试：窄路、侧边障碍、前方长障碍。
- 记录每次测试的障碍位置、速度、costmap 表现、车体行为。
- 调参目标：不贴墙、不硬挤，动态障碍出现时能减速、停车或绕行。

产出：

- `实时避障测试表.md`
- 一组调参前后 RViz 截图或视频

验收标准：

- 障碍进入空路后，`map -> gimbal_yaw_fake` 不整体跳变。
- local/global costmap 能标出障碍。
- 机器人不会把定位错位当成可走区域去撞墙。

### 第 4 周：定位稳定性专项

学习重点：

- Point-LIO 的输入输出
- `registered_scan`、`cloud_registered`、`terrain_map`、`terrain_map_ext` 的区别
- ICP / GICP / NDT 的基本思想
- 重定位中的误差、内点数、跳变拒绝

项目动作：

- 检查 small_gicp 日志中的 accepted / rejected。
- 验证动态障碍只影响 costmap，不拉偏 `map -> odom`。
- 用实车案例写清楚“定位点云”和“避障点云”为什么要分开。

产出：

- `定位漂移与重定位失败排障手册.md`

验收标准：

- 能解释为什么长期扩展地形点云不适合作为默认重定位输入。
- 能讲清 GICP 防跳变保护的意义：误差过大、内点过少、TF 突然跳变时拒绝更新。

### 第 5 周：底盘控制与串口协议

学习重点：

- ROS 坐标系：`x` 前、`y` 左、`z` 上。
- 底盘坐标系和固件速度接口。
- `/cmd_vel` 到串口协议的转换。
- A 板、C 板之间的职责边界。

项目动作：

- 梳理 `/cmd_vel -> chassis_bridge_node -> /dev/ttyABoard -> A板 -> C板`。
- 做三轴测试：`x+` 前进、`y+` 雷达侧、`wz+` 逆时针。
- 记录底盘只转不平移、串口失效、速度方向反向时的排查路径。

产出：

- `底盘通信说明.md`
- `速度方向校准说明.md`

验收标准：

- 能解释 `chassis_bridge_nav.yaml` 里的速度映射矩阵。
- 能说明“ROS2 有速度但车不动”时应按哪几层排查。

### 第 6 周：行为树与比赛式决策入门

学习重点：

- Nav2 BT Navigator
- `NavigateToPose`、`NavigateThroughPoses`、`FollowWaypoints`
- 堵路等待、重新规划、失败退出、恢复行为

项目动作：

- 先阅读当前 BT XML 和 Nav2 行为树配置。
- 设计 RM 场景下的基础策略：遇到动态障碍时等待、绕行、切换目标点或退出。
- 记录 action 状态和 BT Navigator 日志。

产出：

- `实时决策树v1设计.md`

验收标准：

- 能说明实时避障和行为树的边界：costmap/controller 先负责近距避障，行为树负责更高层的任务决策。
- 能设计一套已知地图下的“堵路处理”策略。

### 第 7 周：工程化与可复现实验

学习重点：

- launch 参数整理
- SOP 文档维护
- rosbag / 视频复现
- GitHub 项目展示

项目动作：

- 检查 README、两份 SOP、已知问题文档是否足够给别人复现。
- 准备演示素材：建图、定位、避障、底盘方向测试、Waypoint。
- 准备没有实车时也能说明项目的截图、日志和录屏。

产出：

- README 完善版
- 2 到 3 分钟项目演示视频
- 一组关键 RViz 截图

验收标准：

- 别人看 README 能明白项目做了什么。
- 面试官看视频能快速看到“能跑、能避障、有实车闭环”。

### 第 8 周：面试准备与简历打磨

学习重点：

- 项目表达
- 常见面试问题
- 简历 bullet
- 故障复盘

项目动作：

- 准备 1 分钟、3 分钟、10 分钟三个版本的项目介绍。
- 准备常见问答：TF 树、Nav2 costmap、Point-LIO、GICP、`cmd_vel`、串口协议、实时避障。
- 把调通项目的过程整理成“问题 -> 定位 -> 修改 -> 验证”的故事。

产出：

- 可投递简历项目段落
- 项目答辩稿
- 面试 Q&A 清单

验收标准：

- 能不看代码讲清项目架构。
- 能回答“为什么这样设计，而不是用底盘里程计接管 TF”。
- 能回答“动态障碍为什么会影响定位，你怎么解决”。

## 3. 每天固定节奏

```text
2 小时：ROS2 / Nav2 / SLAM 理论
3-4 小时：围绕实车项目实验、调参或验证
1 小时：写文档，记录命令、现象、原因、结论
每周至少 1 次：录屏或拍视频，留下可展示证据
```

不要只看教程，也不要只调车。每天都要同时推进“理解、验证、记录”三件事。

## 4. 企业最看重的能力

企业不会只看你会不会启动 Nav2，更看重你是否能闭环解决真实问题：

- 传感器链路：能接雷达、IMU、点云、TF，并判断数据是否正常。
- ROS2 工程：能写 launch、参数文件、节点，能查 topic/action/lifecycle/QoS。
- 导航调试：能分析地图、定位、costmap、controller、BT Navigator。
- 实车能力：能处理串口、底盘方向、速度映射、硬件异常。
- 表达能力：能把问题讲成工程案例，而不是流水账。

## 5. 简历项目表达模板

可以把项目写成类似下面的结构：

```text
基于 ROS2 Humble 搭建 MID360 哨兵机器人宿主机导航栈，实现 Point-LIO 建图、small_gicp 重定位、Nav2 低速导航与实时避障。
负责将开源 Docker 导航链路迁移到宿主机，固化实车 TF、地图资产管理和一键启动 SOP。
设计 /cmd_vel 到 A 板串口协议的底盘桥接节点，完成 ROS 坐标系到底盘坐标系的速度映射和三轴方向校准。
针对动态障碍导致重定位漂移的问题，将定位点云与避障点云分离，并增加 GICP 跳变拒绝保护，提升已知地图下导航稳定性。
完成实车建图、定位、Waypoint、多点导航、动态障碍避障测试，并形成可复现文档和演示素材。
```

## 6. 每周复盘问题

每周结束时回答这 5 个问题：

```text
1. 本周我能新解释清楚哪个导航概念？
2. 本周我在实车上验证了哪个现象？
3. 本周我新增了哪个可展示证据？
4. 如果面试官追问，我最容易卡在哪？
5. 下周最值得解决的一个问题是什么？
```

## 7. 当前优先级

短期不要分散到太多方向。优先级如下：

```text
第一优先级：把当前 MID360/Nav2 项目讲清、录好、文档化。
第二优先级：补 ROS2/Nav2 基础，能解释每个节点和话题。
第三优先级：强化实时避障和行为树，形成下一阶段亮点。
第四优先级：再拓展多传感器融合、云台动态 TF、复杂比赛决策。
```

这条路线对实习最划算：先把一个真实项目做扎实，再用它牵引理论学习。项目是你的钩子，理论是你能把钩子讲明白的底气。
