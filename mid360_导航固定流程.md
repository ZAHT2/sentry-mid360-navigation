# MID360 低速导航固定流程

这份文档是唯一的日常低速导航入口。导航运行在宿主机 `/home/zaht/sentry_nav_ws`，不依赖 Docker，也不依赖 Docker 和宿主机之间互传 `/cmd_vel`。

当前稳定路线：

```text
MID360 -> Point-LIO -> small_gicp -> map/odom/base_footprint
Nav2 -> /cmd_vel -> chassis_bridge_node -> /dev/ttyABoard -> A板
```

定位以雷达为准。底盘轮式里程计只发布 `/chassis/odom_raw` 做监控，不发布 `odom -> base_footprint`，不参与导航 TF。

## 0. 关键字说明

```text
MAP_ID
  地图编号。MAP_ID=13 会使用 real_map13.yaml 和 real_scans13.pcd。

FASTDDS_BUILTIN_TRANSPORTS=UDPv4
RMW_FASTRTPS_USE_SHM=0
  强制 ROS2/FastDDS 不走共享内存，避开 fastrtps_port open_and_lock_file 导致的半启动。

source install/setup.bash
  加载 sentry_nav_ws 编译出来的本地包，例如 sentry_bringup、pb2025_nav_bringup、sentry_hardware。

map11_navigation.launch.py
  宿主机导航入口。文件名保留历史名字，但通过 map:= 和 prior_pcd_file:= 可以加载任意地图编号。

use_rviz:=True
  启动 RViz。

use_composition:=False
  Nav2 使用独立进程，便于看日志和定位 lifecycle 问题。

serial_port:=/dev/ttyABoard
  A 板串口。底盘桥从 /cmd_vel 收速度后，通过这个串口发给 A 板。

/cmd_vel
  Nav2 最终速度命令。底盘桥必须订阅到它。

/chassis/odom_raw
  A 板回传的底盘监控里程计。只做监控，不参与导航 TF。
```

## 1. 地图资产和固定 TF

当前稳定示例使用 `map13`。后续换图只改 `MAP_ID`。

```text
MAP_ID=13

2D 地图: /home/zaht/sentry_nav_ws/real_maps/real_map13.yaml
2D 图像: /home/zaht/sentry_nav_ws/real_maps/real_map13.pgm
预览图: /home/zaht/sentry_nav_ws/real_maps/real_map13.png
3D 点云: /home/zaht/sentry_nav_ws/real_maps/real_scans13.pcd
```

固定 TF：

```text
base_footprint -> front_mid360:
  x=-0.001, y=+0.17185, z=0.200
  roll=0.7854, pitch=0, yaw=0

base_footprint -> gimbal_yaw:
  identity

gimbal_yaw -> gimbal_yaw_fake:
  identity
```

`front_mid360` 的 `y` 必须是 `+0.17185`，不是早期错误的 `-0.17185`。`gimbal_yaw_fake` 必须是 identity，不再加旋转角度。

## 2. 通用三终端启动

### 终端 1：清理

```bash
cd /home/zaht/sentry_nav_ws

pkill -9 -f '[m]ap11_navigation|[r]m_navigation_reality|[r]viz2|[c]hassis_bridge_node|[p]ointlio_mapping|[s]mall_gicp_relocalization_node|[l]ivox_ros_driver2_node|[s]tatic_transform_publisher|[l]ifecycle_manager|[m]ap_server|[c]ontroller_server|[p]lanner_server|[b]t_navigator|[v]elocity_smoother|[b]ehavior_server|[s]moother_server|[w]aypoint_follower|[t]errainAnalysis|[t]errainAnalysisExt|[s]ensor_scan_generation_node|[s]ync_slam_toolbox_node' || true

export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
source /opt/ros/humble/setup.bash
ros2 daemon stop || true
rm -f /dev/shm/fastrtps_* /dev/shm/sem.fastrtps* /dev/shm/fastdds* /dev/shm/sem.fastdds* 2>/dev/null || true
```

旧 Nav2 lifecycle、旧 RViz、旧 TF、旧 DDS 端口残留会造成 `inactive/unconfigured`、TF 抖动、地图空白或半启动。一旦哪里不通，优先清理后重启整条链路。

### 终端 2：启动导航

```bash
cd /home/zaht/sentry_nav_ws

export MAP_ID=15
export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 launch sentry_bringup map11_navigation.launch.py \
  use_rviz:=True \
  use_composition:=False \
  map:=/home/zaht/sentry_nav_ws/real_maps/real_map${MAP_ID}.yaml \
  prior_pcd_file:=/home/zaht/sentry_nav_ws/real_maps/real_scans${MAP_ID}.pcd \
  serial_port:=/dev/ttyABoard
```

export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
ros2 launch sentry_bringup map11_navigation.launch.py \
 use_rviz:=True \
 use_composition:=False \
 map:=/home/rciazzx/zaht/sentry-mid360-navigation/maps/small_room_end.yaml \
 prior_pcd_file:=/home/rciazzx/zaht/sentry-mid360-navigation/maps/small_room_end.pcd \
 serial_port:=/dev/ttyACM0

启动后等待 `20s` 再巡检。Nav2 lifecycle 已经做过延迟处理，目的是等 Point-LIO、GICP、TF、地图服务先稳定。

### 终端 3：巡检

```bash
cd /home/zaht/sentry_nav_ws

export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 lifecycle get /map_server
ros2 lifecycle get /controller_server
ros2 lifecycle get /planner_server
ros2 lifecycle get /bt_navigator

ros2 topic info /map
ros2 topic info /cmd_vel
timeout 5 ros2 topic hz /livox/lidar
timeout 5 ros2 topic hz /cloud_registered
timeout 5 ros2 topic hz /chassis/odom_raw
timeout 4 ros2 run tf2_ros tf2_echo base_footprint front_mid360
timeout 4 ros2 run tf2_ros tf2_echo map gimbal_yaw_fake
```

正常判据：

```text
/map_server active [3]
/controller_server active [3]
/planner_server active [3]
/bt_navigator active [3]
/map Publisher count = 1
/cmd_vel Subscription count >= 1
/livox/lidar 约 20Hz
/cloud_registered 约 20Hz
/chassis/odom_raw 约 50Hz
base_footprint -> front_mid360: y=+0.17185, roll=0.7854
map -> gimbal_yaw_fake 能持续输出
```

如果 `/map_server` 是 `active`，但 `controller_server / planner_server / bt_navigator` 停在 `inactive`，先不要点 goal。执行下面的补激活命令：

```bash
cd /home/zaht/sentry_nav_ws

export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 lifecycle set /planner_server activate
ros2 lifecycle set /controller_server activate
ros2 lifecycle set /bt_navigator activate
ros2 lifecycle set /smoother_server activate
ros2 lifecycle set /behavior_server activate
ros2 lifecycle set /waypoint_follower configure
ros2 lifecycle set /waypoint_follower activate
ros2 lifecycle set /velocity_smoother configure
ros2 lifecycle set /velocity_smoother activate
```

补激活后重新执行本节巡检。仍不 active 时，执行终端 1 清理并重启整条链路。

## 3. 后台启动版本

需要后台运行时使用这一套。日志会写到 `/tmp/map${MAP_ID}_host_nav.log`。

```bash
cd /home/zaht/sentry_nav_ws

export MAP_ID=13
: > "/tmp/map${MAP_ID}_host_nav.log"

setsid bash -lc '
export PATH=/opt/ros/humble/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
unset CONDA_EXE CONDA_PREFIX CONDA_PYTHON_EXE CONDA_PROMPT_MODIFIER PYTHONHOME
export LD_LIBRARY_PATH=/opt/ros/humble/opt/rviz_ogre_vendor/lib:/opt/ros/humble/lib/x86_64-linux-gnu:/opt/ros/humble/lib
export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
source /opt/ros/humble/setup.bash
source /home/zaht/sentry_nav_ws/install/setup.bash
exec ros2 launch sentry_bringup map11_navigation.launch.py \
  use_rviz:=True \
  use_composition:=False \
  map:=/home/zaht/sentry_nav_ws/real_maps/real_map${MAP_ID}.yaml \
  prior_pcd_file:=/home/zaht/sentry_nav_ws/real_maps/real_scans${MAP_ID}.pcd \
  serial_port:=/dev/ttyABoard
' </dev/null >"/tmp/map${MAP_ID}_host_nav.log" 2>&1 &

echo $! > "/tmp/map${MAP_ID}_host_nav.pid"
```

查看日志：

```bash
MAP_ID=13
tail -f "/tmp/map${MAP_ID}_host_nav.log"
```

只看异常和速度链路：

```bash
MAP_ID=13
grep -aE 'ERROR|WARN|Failed|GICP|Received /cmd_vel|Wrote chassis cmd|Serial write failed|Opened serial|No map received|Failed to make progress|out of bounds|inactive|unconfigured' "/tmp/map${MAP_ID}_host_nav.log" | tail -n 160
```

## 4. RViz 操作顺序

```text
1. 等 Navigation 和 Localization 变成 active。
2. 用 2D Pose Estimate 点真实位置和车头方向。
3. 观察点云和地图是否在当前位置附近重合。
4. 先给 0.3 到 0.5m 小目标，不要一开始就给远点。
5. 确认 /cmd_vel 非零，底盘方向正确。
6. 到达目标后 /cmd_vel 应回零，TF、点云、地图不能乱跳。
```

连续使用 Waypoint 时：

```text
先点 Cancel。
再点 Reset。
重新进入 Waypoint / Nav Through Poses Mode。
重新按顺序点新的目标点。
最后 Start Navigation。
```

如果 RViz 按钮状态异常，先执行终端 1 清理，再从终端 2 重新启动。

如果不能发布 Waypoint，先查 action server：

```bash
ros2 action list -t | grep -E 'follow_waypoints|navigate_through_poses|navigate_to_pose'
ros2 node info /waypoint_follower
ros2 node info /bt_navigator
```

正确状态：

```text
/follow_waypoints 存在
/navigate_through_poses 存在
/navigate_to_pose 存在
waypoint_follower 里有 /follow_waypoints Action Server
bt_navigator 里有 /navigate_through_poses 和 /navigate_to_pose Action Server
```

如果 action server 存在但 RViz 仍不能发，通常是 RViz 面板还停留在刚启动时的 `unknown` 状态。先点 `Cancel`、`Reset`，重新进入 Waypoint 模式；不行就只重启 RViz 或执行第 2 节完整重启。

如果看不到正常点云，先确认点云话题是否有数据：

```bash
timeout 5 ros2 topic hz /registered_scan
timeout 5 ros2 topic hz /sensor_scan
ros2 topic info /sensor_scan
```

正确状态：

```text
/registered_scan 约 20Hz
/sensor_scan 约 20Hz
/sensor_scan Subscription count >= 1
```

如果 `/sensor_scan` 有 20Hz 但 `Subscription count: 0`，说明 RViz 里的 `Sensor Scan` 显示层没打开。勾选左侧 Displays 里的 `Sensor Scan`，或重启 RViz 读取默认配置。

## 5. 实时避障测试

实时避障阶段固定云台，不改行为树。目标是让动态障碍只进入 costmap，不再把 `map -> odom` 重定位拉偏。

当前分工：

```text
定位输入:
  small_gicp 使用 /registered_scan。

避障输入:
  local_costmap 使用 /terrain_map。
  global_costmap 使用 /terrain_map_ext。
```

巡检话题：

```bash
cd /home/zaht/sentry_nav_ws
export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
source /opt/ros/humble/setup.bash
source install/setup.bash

timeout 5 ros2 topic hz /registered_scan
timeout 5 ros2 topic hz /terrain_map
timeout 5 ros2 topic hz /terrain_map_ext
timeout 8 ros2 run tf2_ros tf2_echo map gimbal_yaw_fake
```

正确状态：

```text
/registered_scan 约 20Hz。
/terrain_map 约 20Hz。
/terrain_map_ext 约 20Hz。
动态障碍出现时，map -> gimbal_yaw_fake 不应突然横向跳变或大角度旋转。
```

看 GICP 是否接受或拒绝本次重定位更新：

```bash
MAP_ID=15
grep -aE 'GICP accepted|GICP rejected|Keeping previous map->odom' "/tmp/map${MAP_ID}_host_nav.log" | tail -n 80
```

动态障碍测试方法：

```text
1. 先在无障碍情况下给 0.3 到 0.5m 小目标，确认定位、规划、底盘执行都正常。
2. 在原本空路里放入较长障碍，占据通道一半以上。
3. 观察 local/global costmap 是否标出新障碍。
4. 观察 map -> gimbal_yaw_fake 是否稳定。
5. 正确行为是减速、停车、局部绕行或规划失败退出，不应贴墙硬挤。
```

失败判据：

```text
障碍出现后地图和点云整体错位。
map -> gimbal_yaw_fake 突然平移或旋转跳变。
车把新障碍当成地图墙体，反而撞向另一侧墙。
```

如果定位不漂，但路径能绕却会撞侧面障碍，说明问题已经从重定位进入“离障距离/侧向安全边界”阶段。优先调这个文件：

```text
src/pb2025_nav_bringup/config/reality/nav2_params.yaml
```

当前使用低速保守离障参数：

```text
local_costmap / global_costmap:
  robot_radius: 0.32
  inflation_radius: 1.0
  cost_scaling_factor: 2.0
  obstacle_min_range: 0.08

controller:
  min_y_velocity_threshold: 0.001
  min_approach_linear_velocity: 0.2
  v_linear_max: 0.8
  v_angular_max: 1.2

velocity_smoother:
  max_velocity: [0.8, 0.8, 1.2]
  max_accel: [0.8, 0.8, 1.5]
```

调参方向：

```text
还会贴侧边障碍：
  robot_radius 继续增大，例如 0.32 -> 0.38。
  inflation_radius 继续增大，例如 1.0 -> 1.2。
  cost_scaling_factor 继续减小，例如 2.0 -> 1.5。

变得太保守、通道过不去：
  robot_radius 小幅减小。
  inflation_radius 小幅减小。
  cost_scaling_factor 小幅增大。

近距离侧障看不见：
  obstacle_min_range 小幅减小，但过小可能把车体自身点云当障碍。
```

## 6. 速度链路检查

只观察 `/cmd_vel`：

```bash
cd /home/zaht/sentry_nav_ws
export FASTDDS_BUILTIN_TRANSPORTS=UDPv4
export RMW_FASTRTPS_USE_SHM=0
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 topic echo /cmd_vel
```

如果 RViz 给了 goal 但车不动：

```bash
MAP_ID=13
ros2 topic info /cmd_vel
timeout 5 ros2 topic hz /chassis/odom_raw
grep -aE 'Received /cmd_vel|Wrote chassis cmd|Serial write failed' "/tmp/map${MAP_ID}_host_nav.log" | tail -n 80
```

解释：

```text
/cmd_vel 有发布、桥日志有 Wrote chassis cmd：ROS 到 A 板下发链路通。
/chassis/odom_raw 约 50Hz：A 板上行回传通。
如果只有 wz 有反应而 vx/vy 不平移，优先查 A->C 转发和 C 板执行逻辑。
```

## 7. 方向约定

实测约定：

```text
ROS/Nav2 x+：车头正前方
ROS/Nav2 y+：雷达侧，左移
ROS/Nav2 wz+：俯视逆时针
```

底盘桥当前映射：

```text
ROS x+ -> chassis_vy+
ROS y+ -> chassis_vx-
ROS wz+ -> chassis_wz+
```

对应参数：

```yaml
cmd_vx_from_ros_x: 0.0
cmd_vx_from_ros_y: -1.0
cmd_vy_from_ros_x: 1.0
cmd_vy_from_ros_y: 0.0
cmd_wz_scale: 1.0
```

方向问题只改 `chassis_bridge_nav.yaml` 的矩阵，不要在 Nav2、临时 `topic pub` 命令或 TF 里反向补偿速度。

## 8. 停止

```bash
cd /home/zaht/sentry_nav_ws
pkill -9 -f '[m]ap11_navigation|[r]viz2|[c]hassis_bridge_node|[p]ointlio_mapping|[s]mall_gicp_relocalization_node|[l]ivox_ros_driver2_node|[s]tatic_transform_publisher|[l]ifecycle_manager|[m]ap_server|[c]ontroller_server|[p]lanner_server|[b]t_navigator|[v]elocity_smoother|[b]ehavior_server|[s]moother_server|[w]aypoint_follower|[t]errainAnalysis|[t]errainAnalysisExt|[s]ensor_scan_generation_node' || true
source /opt/ros/humble/setup.bash
ros2 daemon stop || true
```

## 9. 常见问题

### FastDDS SHM 报错，Nav2 半启动

现象：

```text
Failed init_port fastrtps_port...
controller_server inactive
planner_server inactive
bt_navigator unconfigured
Navigation unknown
```

处理：

```text
启动终端和巡检终端都必须带：
FASTDDS_BUILTIN_TRANSPORTS=UDPv4
RMW_FASTRTPS_USE_SHM=0
```

然后执行第 2 节重新清理和启动。

### 点云和地图左右相反

检查：

```bash
timeout 4 ros2 run tf2_ros tf2_echo base_footprint front_mid360
```

正常：

```text
Translation y = +0.17185
Rotation roll = 0.7854
```

如果 y 是 `-0.17185`，重定位会把空白可走区匹配到障碍区。

### `gimbal_yaw_fake` 方向不对

正确状态：

```text
base_footprint -> gimbal_yaw 是 identity。
gimbal_yaw -> gimbal_yaw_fake 是 identity。
不要再给 gimbal_yaw_fake 添加 yaw。
```

验证：

```bash
timeout 4 ros2 run tf2_ros tf2_echo base_footprint gimbal_yaw
timeout 4 ros2 run tf2_ros tf2_echo gimbal_yaw gimbal_yaw_fake
```

### Nav2 报 obstacle 或 out of bounds

现象：

```text
Either of the start or goal pose are an obstacle
Robot is out of bounds of the costmap
Could not generate path between the given poses
```

处理：

```text
重新点 2D Pose Estimate 到真实空旷位置。
确认点云和地图重合。
目标点不要点在黑色障碍、紫色膨胀层或地图边界外。
先给 0.3 到 0.5m 小目标验证。
```

### 串口打不开或 `Serial write failed`

检查：

```bash
ls -l /dev/ttyABoard /dev/ttyACM*
readlink -f /dev/ttyABoard
fuser -v /dev/ttyABoard "$(readlink -f /dev/ttyABoard)" 2>&1 || true
```

处理：

```text
A 板 USB 重新插拔后，旧底盘桥可能握着失效句柄。
执行第 2 节终端 1 清理，再重新启动导航。
```

如果 `/dev/ttyABoard` 和 `/dev/ttyACM*` 都不存在，说明宿主机没有识别到 A 板 USB。此时 `/cmd_vel` 可以有输出，但底盘不会执行，也不会有 `/chassis/odom_raw` 的 50Hz 回传。

### 底盘只转不平移

判断：

```text
如果 /cmd_vel 有输出、桥日志有 Wrote chassis cmd、/chassis/odom_raw 约 50Hz，但只有 wz 有反应，ROS 到 A 板下发链路不是主因。
```

优先排查：

```text
A 板是否解析 0xA5 0x02。
A 板是否把 vx/vy/wz 完整转发给 C 板。
C 板是否接收到 Nav_X/Nav_Y/Nav_Wz。
ChassisTask::AutoTarget() 是否把 Nav_X/Nav_Y 接入轮速解算。
遥控器双中档是否只允许 wz，但屏蔽平移。
```
